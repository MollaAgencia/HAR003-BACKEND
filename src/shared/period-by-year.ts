export const monthCommercialPeriods = () => {
  return Array.from({ length: 12 }, (_, i) => i + 1)
}

export const quarterlyCommercialPeriods = () => {
  return Array.from({ length: 4 }, (_, i) => i + 1)
}
